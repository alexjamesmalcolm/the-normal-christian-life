<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="color-scheme" content="dark light" />
    <title>{{ page.title }}</title>
    <meta name="description" content="{{ page.description }}" />
    <link rel="stylesheet" href="{{
      "/assets/main.css" | relative_url
    }}" />
  </head>
  <body>
    {% assign next_index = page.order | to_integer %} {% assign prev_index =
    page.order | to_integer | minus: 2 %} {% assign next_article =
    site.articles[next_index] %} {% assign prev_article =
    site.articles[prev_index] %}
    <nav>
      <ul>
        {% if prev_index >= 0 %}
        <li>
          <a href="{{ prev_article.url | relative_url }}">Previous Issue</a>
        </li>
        {% endif %}
        <li>
          <a href="{{ site.baseurl }}/">Table of Contents</a>
        </li>
        {% if next_article %}
        <li>
          <a href="{{ next_article.url | relative_url }}">Next Issue</a>
        </li>
        {%endif%}
      </ul>
    </nav>
    <article>
      <h1>{{ page.title }}</h1>
      <div>
        <cite
          >Nee, Watchman. “The Normal Christian Life.”
          <i>A Witness and a Testimony</i>, {{ page.citation.year }}, pp.
          {{ page.citation.pages }},
          <a rel="noopener" target="_blank" href="{{ page.citation.pdfUrl }}">{{
            page.citation.pdfUrl
          }}</a
          >. Accessed {{ page.citation.accessDate }}.</cite
        >
      </div>
      {{ content }}
    </article>
    <nav>
      <ul>
        {% if prev_index >= 0 %}
        <li>
          <a href="{{ prev_article.url | relative_url }}">Previous Issue</a>
        </li>
        {% endif %}
        <li>
          <a href="{{ site.baseurl }}/">Table of Contents</a>
        </li>
        {% if next_article %}
        <li>
          <a href="{{ next_article.url | relative_url }}">Next Issue</a>
        </li>
        {%endif%}
      </ul>
    </nav>
  </body>
</html>
